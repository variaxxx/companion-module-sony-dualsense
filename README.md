# companion-module-sony-dualsense

## Setup

Build Companion module with:
```
yarn package
```
Create connection in Companion

Then configure `.env` and start bridge:
```
npm i
sudo npm run start
```

sudo is required to get access to DualSense